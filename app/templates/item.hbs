<h4>{{model.name}}</h4>
<img class="item__image" src="/img/{{model.image}}" alt={{model.name}}>
<p>{{model.rating}}</p>
<p>{{model.description}}</p>
<p>
  {{ link-to 'Nutrition' 'item.nutrition' }}
</p>

{{outlet}}
